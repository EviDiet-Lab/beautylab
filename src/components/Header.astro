---
// BeautyLab Header
const siteTitle = "BeautyLab";
const navItems = [
  { label: "ホーム", href: "/beautylab/" },
  { label: "ダイエット", href: "/beautylab/categories/diet" },
  { label: "運動", href: "/beautylab/categories/exercise" },
  { label: "レシピ", href: "/beautylab/categories/recipe" },
  { label: "メンタル", href: "/beautylab/categories/mental" },
  { label: "睡眠", href: "/beautylab/categories/sleep" },
  { label: "お問い合わせ", href: "/beautylab/contact" },
];
---

<header class="site-header">
  <div class="container-wide header-inner">
    <a href="/beautylab/" class="site-logo">
      <span class="logo-icon">✨</span>
      <span class="logo-text">{siteTitle}</span>
    </a>
    <nav class="site-nav">
      <input type="checkbox" id="nav-toggle" class="nav-toggle" />
      <label for="nav-toggle" class="nav-hamburger">
        <span></span><span></span><span></span>
      </label>
      <ul class="nav-list">
        {navItems.map(item => (
          <li><a href={item.href}>{item.label}</a></li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style is:global>
  .site-header {
    background: var(--color-bg-card);
    border-bottom: 2px solid var(--color-border);
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(12px);
    background: rgba(255,255,255,0.92);
  }
  .header-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-md) var(--space-md);
  }
  .site-logo {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    text-decoration: none;
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    color: var(--color-accent);
    font-weight: 700;
  }
  .site-logo:hover { opacity: 0.8; }
  .logo-icon { font-size: 1.4rem; }
  .nav-list {
    display: flex;
    gap: var(--space-sm);
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .nav-list a {
    display: block;
    padding: var(--space-sm) var(--space-md);
    color: var(--color-text);
    font-size: var(--font-size-sm);
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.2s;
  }
  .nav-list a:hover {
    background: var(--color-primary);
    opacity: 1;
  }
  .nav-toggle { display: none; }
  .nav-hamburger {
    display: none;
    flex-direction: column;
    gap: 4px;
    cursor: pointer;
    padding: var(--space-sm);
  }
  .nav-hamburger span {
    display: block;
    width: 24px;
    height: 3px;
    background: var(--color-accent);
    border-radius: 3px;
    transition: 0.2s;
  }
  @media (max-width: 768px) {
    .nav-list {
      display: none;
      flex-direction: column;
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: var(--color-bg-card);
      border-bottom: 2px solid var(--color-border);
      padding: var(--space-md);
    }
    .nav-toggle:checked ~ .nav-list {
      display: flex;
    }
    .nav-hamburger {
      display: flex;
    }
  }
</style>
